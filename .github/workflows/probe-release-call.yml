name: Probe release call (temporary, bd-The-Claude-Protocol-lvg)

# Temporary: calls test.yml the way release.yml does, with a stand-in for
# publish, so the release path is checked without a tag. Removed in the next
# commit.
on:
  push:
    branches: [bd-The-Claude-Protocol-lvg]

jobs:
  test:
    uses: ./.github/workflows/test.yml

  publish-stand-in:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - run: echo "publish would start here"
